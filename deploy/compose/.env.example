# Required: bootstrap secret. Add as:
#   SHOEBOX_SECRET=$(openssl rand -base64 24)
# (See README Step 2 for the one-liner.)

# Required: absolute host path to the photos library.
SHOEBOX_PHOTOS_DIR=/srv/photos

# Optional: log level (info | debug | trace).
SHOEBOX_LOG=info
